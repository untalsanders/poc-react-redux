const config = {
    api: {
        baseUrl: 'http://localhost:3000',
    },
    app: {
        name: 'My React App (DDD)',
        version: '1.0.0',
    },
    environment: 'development'
}

export default config
